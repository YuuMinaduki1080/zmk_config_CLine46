# 2026-08-06

## 00:41 feat/auto-mouse-layer

キーマップを同じ作者の別キーボード LiNEA40 の構成へ全面移行した。8レイヤー構成（default / MARK / FUNCTION / CURSOR / AML / MOUSE / SCROLL / WIRELESS）、コンボ8個、マクロ2個。あわせてレイヤー番号の総入れ替えに追随して `CLine46_R.overlay` を3箇所修正した。

### 物理配列の差分をどう埋めたか

CLine46 46キー（6列×2）と LiNEA40 41キー（5列×2＋エンコーダ）で配列が違うため 1:1 コピーは不可。英字ブロックを 1:1 対応させ、段ごとに異なるオフセット（1段目 +1、2段目 +3、3段目 +5）で位置を対応付けた。CLine46 の余剰最外列（左右各1列・計6キー）と右親指の差分はユーザーが個別に決定。

### 設計判断

- **日本語入力キー（無変換・変換）を廃止**し、LiNEA40 の親指配列に揃えた
- **ホームロウモッドは採用しない**。修飾キーを専用の物理キー（左右 Shift・Win・Alt・Ctrl）に置いた。これにより LiNEA40 の `hmr_*` 6種と `tog_off` / `exit_AML` / `kp_exit_AML` の移植が丸ごと不要になった
- **`kp_exit_AML` を移植しない判断の根拠**: LiNEA40 ではホームロウモッドのタップ側に仕込まれ、「ホールド＝修飾キーなので AML 維持／タップ＝文字入力なので AML 解除」を区別していた。`excluded-positions` だけではこの区別ができないための仕掛け。修飾キーを専用の物理キーにした本構成では区別自体が不要で、`Space`(40) と `BackSpace`(41) を除外リストに入れないことで「打鍵した瞬間に解除」が素の設定で得られる
- **手動 MOUSE レイヤー(5) は定義のみ残し到達手段を設けない**（ユーザー了承済み）。LiNEA40 の `&to 5` は右親指5→4の枠不足で落とした
- **コンボに `layers = <0>` を付けた**。ZMK のコンボは `layers` 省略時に全レイヤーで有効になり、SCROLL のテンキーで「45」を素早く打つと `pipe` コンボが `|` を出す、AML で左右クリック同時押しが `|` になる、という実害があった。LiNEA40 も同じ状態だが、受け継いだ不具合であって意図した挙動ではないと判断した

### overlay 側の追随（レビューで最重視した箇所）

レイヤー番号が総入れ替えになったため、番号で参照している overlay を3箇所直した。**keymap だけ直して overlay を放置してもビルドは通り、無言で誤動作する**（旧値のままだと `zip_temp_layer 5` が到達不能なはずの MOUSE を有効化し、`layers = <3>` が CURSOR 保持中のボール操作をスクロール化する）。

- `<&zip_temp_layer 5 300>` → `<&zip_temp_layer 4 300>`（AML=4）
- `scroller` の `layers = <3>` → `<6>`（SCROLL=6）
- `excluded-positions` を新配置に合わせて13個へ差し替え

### レビュー（Tier 2・2名並列）

両名 APPROVE。コンボのレイヤー制限漏れは両名が独立に指摘し、レビュアーの提示した修正案をそのまま適用した。コンボの行末コメント（物理キー名）も復活させた。位置番号の付け替えは段ごとにオフセットが違い、このリポジトリで過去に一度実際に腐っている箇所のため、コメントが唯一の再検証手掛かりになる。

### LiNEA40 から受け継いだ、実装ミスではない挙動（ユーザーへ報告済み・未決）

- **スクロール中はクリックできない**。SCROLL(6) > AML(4) のため `&mo 6` 保持中は位置19・20 が `4` `5` になる。旧構成は AML(5) > SCROLL(3) でクリックできていたので後退
- **ボール操作後300ms 以内の `M` `,` `.` が `Ctrl+F4` / `Alt+←` / `Alt+→` になる**。しかもこの3つは `excluded-positions` に入っているため誤爆しても AML が解除されず連鎖しうる。`require-prior-idle-ms = 200` は「打鍵→ボール」方向しか守らない
- **CURSOR(3) は `&to 3` で入る永続レイヤーのため、矢印クラスタ（位置19・20・21）が定常的に AML に潰される**
- `&kp UNDER`（SCROLL 位置30）は `LS(MINUS)` の別名で、JIS 配列では `_` ではなく `=` が出る
- `BT_CLR_ALL`（全ペアリング消去）が右手だけで押せる（`&mo 7` は右親指、位置35 は右小指）
- SCROLL の位置43・44 が両方 `&kp NUMBER_0`。位置44 は通常 `&mo 7` なので、スクロール中は WIRELESS に入れない

### 未完了

- ビルド確認（push 後の GitHub Actions。左右両方の success が条件）
- 実機での動作確認。AML 発動中のクリックは **`J`=左・`K`=右・`L`=スクロール**（AML 単独実装のときの `J`/`L` から変わっている）
- 上記「受け継いだ挙動」6点はユーザー判断待ち

## 00:58 feat/auto-mouse-layer

ユーザー依頼で2箇所を調整した。

- **スクロールのY軸を反転**: `scroller` 子ノードで有効だった `<&zip_xy_transform INPUT_TRANSFORM_Y_INVERT>` をコメントアウトした
- **MARK レイヤーの `:` と `;` を入れ替え**: 位置22 を `&kp SINGLE_QUOTE`(JIS で `:`)、位置23 を `&kp SEMICOLON`(JIS で `;`) にした

### Y軸反転の実装方法についての注意（将来の事故予防）

`scroller` ノードには Y 反転の候補行が**2つ**、いずれも無効状態で並んでいる。

- `//<&zip_xy_transform INPUT_TRANSFORM_Y_INVERT>` — スクロール変換の**前**に生の Y を反転する
- `//<&zip_scroll_transform INPUT_TRANSFORM_Y_INVERT>` — スクロール変換の**後**にホイール出力を反転する

**この2つは排他で使うこと。** 後日「有効化すれば反転する」と見て両方を有効にすると、反転が2回掛かって打ち消し合い、無反転に戻る。今回は前者を無効化する形で反転を実現しており、チェーン全体に Y 反転は1つも残っていない。

今回は反転の実現手段として「後者を有効化する」案も取れたが、それだと反転が2回掛かって打ち消し合う構成になり、読み手に意図が伝わらないため採らなかった。

### レビュー（Tier 1・1名）

APPROVE。列アラインメントが3文字ずれた点を Minor 指摘されたので修正した。このリポジトリは全レイヤーで列を厳密に揃えており、キー位置を数え直す際の唯一の手掛かりになるため、ずれは今後の位置ミスの誘因になる。

## 01:25 feat/auto-mouse-layer -> main

一連の作業を `main` へマージした。ユーザーが「LiNEA40 から受け継いだ挙動は特に違和感ない」と判断し、6点すべてを現状のまま受け入れることになったため。

受け入れが確定した挙動（前エントリの一覧のとおり。今後の調査で「なぜこうなっているのか」を辿れるよう再掲せず参照に留める）:
スクロール中はクリックできない / ボール操作直後の `M` `,` `.` が破壊的コマンドになる / CURSOR の矢印クラスタが AML に潰される / `&kp UNDER` が JIS で `=` を出す / `BT_CLR_ALL` が右手だけで押せる / SCROLL の位置43・44 が両方 `NUMBER_0`。

`--no-ff` でマージコミットを残した。ファストフォワード可能な状態だったが、実機で使い込んだ結果まとめて戻したくなった場合に、マージコミット1つの revert で一括して戻せるようにするため。

### この時点でのブランチ構成

- `main`: 一連の作業を取り込み済み
- `feat/auto-mouse-layer`: 削除せず残す。マージ前の各段階へ戻る目印として機能する
- フォーク元 `takamaru-fpv/zmk_config_CLine46` は `west.yml` が `revision: main` のままなので、今ビルドすれば依存の腐りで失敗する状態が続いている。本フォークはコミット固定済みでビルドが通る

## 02:12 fix/pmw3610-rest-sample-time

「しばらく放置した後、トラックボールを動かし始めると最初だけ反応しない」というユーザー報告の原因を PMW3610 の省電力ダウンシフトと特定し、`CLine46_R.conf` で `REST2_SAMPLE_TIME_MS=20` / `REST3_SAMPLE_TIME_MS=100` を明示した。あわせて実在しない Kconfig 名のコメント行を削除し、根拠と出典をコメントに残した。

### 原因

REST2/REST3 のサンプル周期が未指定＝ドライバ既定（100ms / 500ms）だった。無操作が続くと RUN→REST1→REST2→REST3 と落ちていき、REST3 では 500ms に1回しかセンサーを読まないため動かし始めが取りこぼされる。到達までの実効時間は 0.48 + 4.8 + 12.8 = 約18.1秒（各レジスタは ms 値を分解能で切り捨てた値を持つ）。ユーザーの体感「20秒以上放置で発生」と一致した。

### 設計判断

- **REST3 は 20ms まで詰めず 100ms に留めた**。体感で気付かない水準を確保しつつ、NiMH 運用でのアイドル電流の増分を抑えるため。REST2 は小休止の頻度が高いため 20ms
- **ダウンシフト時間を延ばす案は採らなかった**。数分の離席には効くが長時間放置には効かず、症状の根に当たらない
- **`force-awake`（devicetree プロパティ）は採らなかった**。ドライバは `zmk_activity_state_changed` を購読しており、ZMK が非アクティブになると強制覚醒を解除する。`CONFIG_ZMK_IDLE_TIMEOUT=30000` のため30秒で解け、長時間放置の症状には効かない
- **REST2 を 20ms にするとダウンシフトの分解能も変わる**（`128 × REST2_SAMPLE`）。既定17000ms の切り捨て先が 12800ms → 15360ms になり、REST3 到達は約18.1秒 → 約20.6秒へ後ろにずれる。狙いに対しては有利側

### コメントの真偽検証（レビューの主戦場）

初回レビューは REQUEST_CHANGES。指摘は設定値ではなく**追加したコメントの事実主張が検証できない**点に集中した。ドライバ（`badjeff/zmk-pmw3610-driver` の `zmk-0.3`）は west 取得でリポジトリに vendor されておらず、レビュアー側には参照手段が無い。メインループ側で `Kconfig` と `src/pmw3610.c` を実際に開いて既定値・有効範囲（10〜2550ms、分解能10ms）・レジスタ換算（切り捨て除算）・Kconfig 項目の全量を確認し、出典と参照日をコメントに明記して再レビューで APPROVE。

- レビュアーが推定した既定値（REST1 ダウンシフト 9600 / REST2 12800）は実測（5000 / 17000）と食い違っていた。conf にコメントアウトで残っていた `=9600` は既定値ではなく他 fork の README 由来の例示値で、既定と誤読させるため値を削除した
- `CONFIG_PMW3610_FORCE_AWAKE` と `CONFIG_PMW3610_REST3_DOWNSHIFT_TIME_MS` はいずれもこのドライバの Kconfig に存在しない。コメントアウトされていたため無害だったが、実在すると誤読させるので削除して「存在しない」旨を注記に置き換えた

### 未完了

- ビルド確認（push 後の GitHub Actions。トラックボールは右手＝セントラル側のため実質 `CLine46_R` のみが対象だが、成功判定は左右両方）
- 実機での動作確認。**25秒以上放置してから**試すこと。本変更で REST3 到達が約20.6秒へ後ろにずれたため、20秒放置では REST2(20ms) を見ることになり、症状が出ないのを対策成功と誤読する
- REST3 での 100ms 分の取りこぼしは仕様上残る。数日運用してアイドル時の電池の減りが許容範囲かも合わせて確認する（悪化が目立てば REST3 を 200ms 程度へ、それでも足りなければ REST2 を 50ms 程度へ）
- `west.yml` の `zmk-pmw3610-driver` は `revision: zmk-0.3`（ブランチ）のまま。コメントの「存在しない」系の主張は上流が動けば無検証で偽になりうる。参照日を書いて凌いだが、SHA 固定は 2026-08-05 の将来課題として残存

## 09:42 fix/pmw3610-rest-sample-time

キーボード制作者の説明（2時間を超えて放置すると、トラックボールを操作しても左側が復帰せず、両側の電源 OFF/ON でしか戻らない）を受けて、左右とも `CONFIG_ZMK_SLEEP=n` にしてディープスリープを無効化した。ユーザーが電池消費の増加を承知のうえで「両側でスリープ無効」を選択。

### 2時間の境界で何が変わるか

- **〜2時間**: `CONFIG_ZMK_IDLE_TIMEOUT=30000` のアイドル状態。センサーは動き続けるのでボールで復帰できる（制作者の記述と一致）
- **2時間〜**: `CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=7200000` によるディープスリープ。左半分は自分の kscan（`CLine46.dtsi` の `kscan0` の `wakeup-source`）でしか起きず、右のボール操作では復帰しない。右も PMW3610 ドライバが IRQ ピンをウェイクアップソースとして登録していないためボールでは起きない

「2時間」は ZMK の既定ではなくこの conf の設定値（既定は 900000ms=15分、`ZMK_SLEEP` 自体は既定 n のオプトイン）。

### 設計判断

- **タイムアウト延長ではなく無効化**を選んだ。延長は先送りにすぎず、長期放置で同じ状態に入る
- **`CONFIG_ZMK_IDLE_TIMEOUT` は変更しない**。アイドル状態はボールで復帰できており、今回の問題とは層が違う
- **`CONFIG_ZMK_IDLE_SLEEP_TIMEOUT` は削除せずコメントアウト**。`if ZMK_SLEEP` 配下のため `ZMK_SLEEP=n` のまま代入すると Kconfig 警告になるが、行を残せば戻すのが 2 行の編集で済む
- **左側のみ無効化する案は採らなかった**。故障機構の推定に基づく案で、外れたときに実機書き込み1往復を失う

### レビュー（Tier 1・1名、2ラウンド）

初回 REQUEST_CHANGES。**確認していた `app/Kconfig` が本家 zmkfirmware/zmk のもので、このビルドが引くのは `west.yml` の `cormoran/zmk` `v0.3-branch+dya`** という指摘。フォーク側を開き直して該当箇所が本家と同一であることを確認し、出典（リポジトリ・リビジョン・参照日）をコメントに明記して再レビューで APPROVE。DYA fork は settings-rpc で「アイドル/スリープ設定」を扱うため、まさにここに手が入っていてもおかしくない層だった。

- 「左側が復帰しない」理由を PMW3610 ドライバのせいと読める書き方だったのを訂正。左半分にトラックボールは無く（`CLine46.dtsi` で `spi0`/`trackball` は disabled、有効化は `CLine46_R.overlay` のみ）、ドライバを直しても左は起きない
- レビュー2巡目で `ZMK_SLEEP` が `POWEROFF` / `ZMK_PM_DEVICE_SUSPEND_RESUME` を select し `USB` を imply することが判明。`n` にするとアイドル時のデバイス suspend/resume も働かなくなる（`spi0_sleep` の pinctrl 状態も未使用になる）。コメントに追記した

### 未完了

- ビルド確認（push 後の GitHub Actions。左右とも success）
- **USB 接続での ZMK Studio 接続と USB キーボード動作の実機確認**。`ZMK_SLEEP` の `imply USB` が外れる影響は、他経路で USB が有効なら無症状で、CI 成功だけでは判定できない
- 2時間超（できれば半日）放置してから、電源 OFF/ON なしで復帰するかの実機確認
- 数日運用して電池の減りが許容範囲か。許容できなければ `ZMK_SLEEP=y` とタイムアウト行を戻せば可逆
- `CONFIG_ZMK_NON_LIPO_ADV_SLEEP_TIMEOUT`（コメントアウト）と Settings RPC の「アイドル/スリープ設定」が `ZMK_SLEEP=n` でゲートされるかは、モジュールが vendor されていないためリポジトリ内から検証できない

## 14:56 fix/pmw3610-rest-sample-time

**前エントリ（09:42）の `CONFIG_ZMK_SLEEP=n` は効かないうえに悪化させていた**。ユーザーが書き込んだうえで「2時間も経たずに左側が復帰しなくなる」と報告し、未検証のまま残していたモジュール側を調べて判明。左右とも `CONFIG_ZMK_SLEEP=y` に戻し、`CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=86400000`（24時間・ユーザー指定）を明示した。

### なぜ効かなかったか

NiMH 用モジュール `takamaru-fpv/zmk-feature-non-lipo-battery-management`（`voltage_division`）の Kconfig:

```
config ZMK_NON_LIPO_BATTERY_MANAGEMENT
    bool "Non-LiPo battery monitoring and management"
    depends on DT_HAS_ZMK_NON_LIPO_BATTERY_ENABLED
    select ZMK_SLEEP
    select ADC
    select ADC_NRFX_SAADC
```

`select ZMK_SLEEP` がある。左右とも `CONFIG_ZMK_NON_LIPO_BATTERY_MANAGEMENT=y` で `CLine46.dtsi` の `non_lipo_battery` ノードも有効（`vbatt` と違って `status` 未指定＝okay）なので、`.conf` の `ZMK_SLEEP=n` は Kconfig 警告が出るだけで y のまま。**そのうえで `CONFIG_ZMK_IDLE_SLEEP_TIMEOUT` をコメントアウトしたため、既定の 900000ms（15分）に落ちた**。2時間→15分の短縮になっていた。

`select` された symbol は .conf で n にできない、という Kconfig の基本を踏んだ事故。値の不一致は警告止まりでビルドは通るため、**CI 成功は何の担保にもならなかった**。

### 調査で判明した、この構成に残る2つの電源断経路

どちらも `ZMK_SLEEP` でゲートされていない。

- **低電圧シャットダウン（生きている）**: `check_voltage_and_shutdown()` が `millivolts <= CONFIG_ZMK_NON_LIPO_LOW_MV`（この設定では 1000mV・モジュール既定は 1050）かつ USB 非給電のとき `zmk_pm_suspend_devices()` の後に `sys_poweroff()` を呼ぶ。ウェイクアップ設定を伴わないため復帰は電源入れ直しかリセット。スリープが実質無効化されたぶん、これが現実的な終端経路になる
- **広告タイムアウトのスリープ（死んでいる）**: ガードが `#if IS_ENABLED(CONFIG_ZMK_NON_LIPO_ADV_SLEEP_TIMEOUT)` だが、この設定は `int`（既定 60000）なので `IS_ENABLED` は 0 になりブロックごとコンパイルから外れる。ハンドラ内の `sys_poweroff()` もコメントアウト済み。**有効にしても動かない**ため、コメントで明示した

### 設計判断

- **`ZMK_SLEEP=y` を明示で戻した**。select される以上 y にしかならず、`=n` を残すと警告源かつ「無効化できている」と誤読させる。実効的な制御はタイムアウト一本に集約する
- **バッテリー管理モジュールを外して select を断つ案は採らなかった**。NiMH の残量表示と低電圧保護を失う
- 86400000 = 24時間ちょうど、int32 の範囲内（`INT32_MAX` の約 4%）

### レビュー（Tier 1・1名、2ラウンド）

初回 REQUEST_CHANGES。設定値ではなくコメントの事実性への指摘。

- 「2時間設定時に電源 OFF/ON でしか戻らなかった」を自己観測のように書いていたが、これは**制作者の説明の伝聞**で、本プロジェクトで 7200000 のまま再現した記録は無い。しかも直後の「左半分は自分の kscan の wakeup-source で起きる」と論理的に衝突する（真なら左キー打鍵で復帰するはず）。出所を明示し、左キー打鍵は未検証と断る形に修正
- 低電圧シャットダウンの境界を「下回ると」と書いていたが実装は `<=`。`!zmk_usb_is_powered()` ガードも落ちていた
- 「24時間にして実際には踏まないようにしている」は無効化と読めるため、先送りである旨を明記

### 未完了

- ビルド確認（push 後の GitHub Actions）。**`ZMK_SLEEP` 系の Kconfig 警告が消えていること**も合わせて見る（前回は `=n` で警告が出ていたはず）
- **無線運用で**の放置テスト。USB 給電中は低電圧シャットダウンもスリープも発動しないため、USB 接続では検証にならない
- ZMK Studio 側でスリープ設定が 24 時間と表示されるか。DYA fork の Settings RPC が「アイドル/スリープ設定」を NVS に保存する実装なら、Kconfig 値は初期値にすぎず旧ファーム由来の保存値（7200000 や 900000）が優先されうる
- 24 時間超の放置では同じ復帰不能が再発する（先送りであって解決ではない）
- `LOW_MV=1000` 到達までの電池持ちの観察。スリープが実質無効化されたぶん早まる
- USB 接続での ZMK Studio 接続と USB キーボード動作（09:42 エントリから継続）
- 25 秒以上放置後のボールの動き出し（02:12 エントリから継続）
- `CLine46_R.conf` の「詳細は『BLE 接続』セクションを参照」は参照先が存在しない（同ファイルにも README にも該当見出しなし）。今回の変更範囲外のため未修正

## 19:08 fix/pmw3610-rest-sample-time

トラックボールのスクロールを縦専用・横専用の 2 キーに分けた。呼び出しは AML 上の位置21（`&mo 6`＝縦、従来どおり）と位置23（`&mo 8`＝横、新設）。

### 実現方法

ZMK に軸ごとのスケーラー `zip_x_scaler` / `zip_y_scaler` があるので、スクロール変換の**前**に片方の軸を 0 倍で潰す。スケーラーの実装は `event->value * mul` を計算するだけで、乗数 0 でもゼロ除算にならず 0 が流れる（イベントを捨てる分岐は無い）。

- `v_scroller`（`layers = <6>`）: `<&zip_x_scaler 0 1>` を先頭に追加
- `h_scroller`（`layers = <8>`・新設）: `<&zip_y_scaler 0 1>` から始める

**順序が本質**。`zip_xy_to_scroll_mapper` の後ろに置くと、既に Y は WHEEL・X は HWHEEL に変換済みで、`codes = <INPUT_REL_X>` / `<INPUT_REL_Y>` にマッチせず何も起きない。

キーマップ側は 9 層目 `HSCROLL`（インデックス8・全 `&trans`）を末尾に追加し、`SCROLL` を `VSCROLL` に改名。overlay のノード名も `scroller` → `v_scroller` に改めた（2026-08-06 の「Y軸反転の実装方法についての注意」節が指す `scroller` は現在の `v_scroller`）。`excluded-positions` に 23 を追加している。**これが無いと押した瞬間に AML が解除されて横スクロール層に入れない**。

### レビュー（Tier 2・2名並列、3ラウンド）

設定値・プロセッサ順序・軸の対応・`excluded-positions` には最後まで指摘ゼロ。**3ラウンドすべてコメントの記述の真偽が争点**だった。

- **1巡目**: 軸を 0 倍で潰したことで、縦チェーンに残した `zip_scroll_transform X_INVERT` が完全な no-op になったのにコメントが「スクロールのX軸反転」のままだった。また、コメントアウトされた候補行が `zip_x_scaler` より前に適用されるため、有効化したときの帰結が変わっていた。どちらもビルドもテストも通る
- **2巡目**: 1巡目の指摘を受けて書いた「XY_SWAP を有効にすると縦スクロールが死ぬ／軸を入れ替えるなら zip_y_scaler へ差し替える」が**両方とも偽**だった。XY_SWAP は軸コードごと入れ替えるので、潰されるのは入れ替え後の X＝元の Y。縦スクロールは元の X（ボールの左右）で駆動され続ける。指示どおり `zip_y_scaler` に差し替えると、縦専用レイヤーが横スクローラーに化ける。**レビュアー自身の 1 巡目の誤りをそのままコメントに固定していた**
- **2巡目・列アラインメント**: 新レイヤー最下段に左右の区切り（5空白）を入れたところ、位置43 が idx6 の列（col64）から 3 文字ずれた。物理配列でも位置43 は idx6 の真下。区切りと列一致は両立しないため、区切りを取り下げて一様 2 空白に戻した

前提が未検証のまま結論だけ書かない、という処理も入れた。`zip_xy_transform` が軸コードを付け替える実装であることはリポジトリ内からもレビュアーからも確認できないので、該当コメントには「実機未検証」と明記している。

### 未完了

- ビルド確認（push 後の GitHub Actions・**左右とも** success）。`zip_x_scaler` / `zip_y_scaler` が未定義なら phandle 未解決で落ちるので、CI がこの検証を兼ねる。9 層目によるレイヤー数上限も同様。左側は `CONFIG_ZMK_STUDIO` 未設定のため、上限の既定値が Studio 有無で変わる実装なら左だけ落ちうる
- 実機: 縦キーで横に転がしても動かないこと／横キーで縦に転がしても動かないこと／**横スクロールの向き**（旧 SCROLL の X 経路と同一なので従来と同じ向きになるはず。逆なら `h_scroller` の `INPUT_TRANSFORM_X_INVERT` を外す）／横の分解能 `1 8` が縦と同値でよいか
- **AML 発動中（ボール操作後300ms）は位置23 の `_` が出ず、`excluded-positions` に入っているため AML も解除されない**。既知の `M` `,` `.` と同種だが破壊的ではない。`_` はコンボ 21+22 で代替可能。ユーザーに明示済み
- レイヤー6と8が同時に有効になったときの挙動は未確認（`zmk,input-listener` の子ノードが最初のマッチだけか全マッチかがリポジトリ内から判定できない。両方適用される実装なら `zip_scroll_scaler 1 8` が二重に掛かって 1/64 になりうる）。なお位置21・23 のどちらを先に押しても、AML が有効な間だけ成立する点は対称
- ZMK Studio にキーマップを保存済みだと NVS 側が優先され、HSCROLL(8) や `&mo 8` が反映されない可能性がある（`firmware/` 同梱の `settings_reset` で対処可能）

## 20:36 fix/pmw3610-rest-sample-time

ユーザー指示で、横スクロールの呼び出しを位置23（`_`）から**位置9（`O`）**へ移した。`excluded-positions` も 23 を外して 9 を追加。縦（位置21）は不変。

### レビューで判明した副作用（ユーザー再提示のうえ受容）

位置9 を除外リストに入れると、ボール操作後300ms 以内に `O` を打ったとき **`O` が出ないだけでなく AML が解除されない**。そのため続けて打った `J` `K` `L` が `&mkp MB1` / `&mkp MB2` / `&mo 6` として発火する。これは 2026-08-05 に一度否決した失敗モードと同型（当時「除外を残すと `k` を打っても AML が解除されず、直後の `j` が左クリックとして誤発火する。打鍵の安全側に倒した」と記録）。位置23 の `_` はコンボ 21+22 で代替できたが、`O` のコンボ `8+9` は `layers = <0>` なので AML 中は発火せず代替が無い。

除外から外す回避は使えない。押した瞬間に AML が解除されて `&mo 8` に到達できなくなる（位置21 が除外に入っているのと同じ事情）。

選択肢（位置9 のまま / 位置11 の `=` / 位置23 に戻す）を提示し、ユーザーは**位置9 のまま**を選択した。実機で誤発火が気になれば 1 行で移せる。

### レイヤー6・8 の共存条件が非対称になった

前エントリで「位置21・23 のどちらを先に押しても対称」と書いたが、**位置9 へ移したことで非対称になった**。

- 成立する: 位置9（`&mo 8`）を保持したまま位置21 を押す。HSCROLL は全 `&trans` なので、AML が有効な間は AML の `&mo 6` に落ちる
- 成立しない: 逆順。VSCROLL の位置9 は `&kp NUMBER_9` で、レイヤー6 > 4 のため `&mo 8` に届かない

レビューでは、位置番号だけを 23→9 に置換して前提が崩れたコメントを放置していた点が Major として指摘された（このリポジトリで繰り返している型）。

### 未完了

- ビルド確認（push 後の GitHub Actions・左右とも success）
- 実機: `O` の位置で横スクロールできること／位置23 で `_` が元どおり打てること／**ボール操作直後に `O` を打ったときの誤発火**の許容可否

## 20:59 fix/pmw3610-rest-sample-time -> main

本日のトラックボールまわりの一連（5コミット）を `main` へマージした。前回と同じく `--no-ff` でマージコミットを残している。実機で使い込んだ結果まとめて戻したくなった場合に、マージコミット1つの revert で一括して戻せるようにするため。

### マージ時点で確認が取れているもの

- REST サンプル周期（`bae24f6`）: 30秒放置後の動き出しに違和感なし
- スリープ閾値24時間（`3d319f4`）: 15分以上放置しても復帰できる
- ZMK Battery Center での左右のバッテリー表示: ファーム側の設定（`ZMK_BATTERY_REPORTING` と split の PROXY / FETCHING）が既に入っており、アプリを入れるだけで動いた

### マージ時点で未確認のもの

- スクロールの縦横分割（`4cf2e02`・`a8b35e6`）は書き込み前。横スクロールの向き・分解能、`O` の位置での誤発火の許容可否が残っている
- 24時間を超える放置での復帰（先送りであって解決ではない）
- スリープを実質無効化したぶんの電池の減り。Battery Center の履歴グラフで観測できる状態にある
- 起動直後にボールの動き出しが少しかくつくというユーザー報告。BLE の接続パラメータ更新待ち（Zephyr の `CONFIG_BT_CONN_PARAM_UPDATE_TIMEOUT`・既定5秒）が疑わしいが未調査。数秒で収まるか、もっと長く続くかで切り分ける

### 履歴に残した失敗

`c17d0ff`（`CONFIG_ZMK_SLEEP=n`）は効かないうえに悪化させたコミットだが、squash せず履歴に残した。`select` された symbol は `.conf` で n にできず、しかも値の不一致は警告止まりでビルドが通る——という再発しやすい事故のため、経緯を辿れる形を優先した。
